APP_NAME=KidTube
APP_VERSION=0.1.0
HOST=0.0.0.0
PORT=2018
DATABASE_URL=sqlite:////data/kidtube.db
LOG_LEVEL=INFO
DISCORD_PUBLIC_KEY=
YOUTUBE_API_KEY=
SYNC_INTERVAL_SECONDS=900
SYNC_MAX_VIDEOS_PER_CHANNEL=15
HTTP_TIMEOUT_SECONDS=10

DISCORD_APPROVAL_WEBHOOK_URL=
# Paste your Discord webhook URL here.
# In Discord: channel settings → Integrations → Webhooks → New Webhook → Copy URL

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
APPROVAL_EMAIL_TO=
# Gmail SMTP: use an App Password (Google Account → Security → App Passwords).
# SMTP_FROM should usually match SMTP_USERNAME.
